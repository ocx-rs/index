<!--
  This file exists only so VitePress emits 404.html — the page body below is
  never rendered. Layout.vue dispatches on `page.isNotFound` (rendering
  theme/NotFound.vue) before it ever consults frontmatter.
-->
